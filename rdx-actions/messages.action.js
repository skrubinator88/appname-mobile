// Store
exports.add = (id, message) => {
  return { type: "ADD", chat_id, message };
};

exports.update = (id, message) => {
  return { type: "UPDATE", chat_id, message };
};

// exports.clear = () => {
//   return { type: "CLEAR" }; // Empty store
// };
